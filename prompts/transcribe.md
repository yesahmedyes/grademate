Transcribe this document into clean, faithful Markdown.
- Preserve headings, numbering, bullet lists and tables (GitHub Markdown).
- Write every mathematical expression in LaTeX: inline as $...$, display as $$...$$.
- Put code in fenced code blocks with a language tag.
- Output ONLY the Markdown — no commentary, no code fence around the whole document.
